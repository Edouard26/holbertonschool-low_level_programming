#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: (int) multiplie with b
 * @b: (int) multiplie with a
 * Return: 1 or 0
 */

int mul(int a, int b)
{
	return (a * b);
}
