#include "main.h"
#include <stdio.h>
/**
 * int _abs(int) - computes the absolute value of an integer
 *
 * Return: Always (0) Success
 */

int _abs(int i)

{

	if (i >= 0)
	{
		return (i);
	}

	else
	{
		return (-i);
	}
}
